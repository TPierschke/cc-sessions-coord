---
name: coord
description: Cross-session coordination via ccsc-channel MCP. Subcommands status, alert, ask, pong, respond, ack, list, cancel, rewrite. See packaging/claude-code/skills/coord/SKILL.md.
---

Load skill **coord**. Use `mcp__ccsc-channel__*` tools only — never `coord-subcommands.ps1`.
